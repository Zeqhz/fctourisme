<nav class="navbar navbar-expand-lg bg-light">
    <a class="navbar-brand" href="#">FC TOURISME</a>
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{ path("app_etablissements") }}">Accueil</a>
            {%  if app.user %}
                <div class="d-flex text-light">
                    <p>{{ app.user.pseudo }}</p>
                    <a class="btn" href="{{ path('app_logout') }}">Déconnexion</a>
                </div>
            {% else %}
                <div class="d-flex text-light">
                    <p>Vous n'êtes pas connecté !</p>
                    <a class="btn" href="{{ path('app_login') }}">Connexion</a>
                </div>
            {% endif %}
        </li>
    </ul>
</nav>